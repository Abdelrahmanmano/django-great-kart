// (function ($) {
//     $(document).ready(function () {
//         function calculateField() {
//             var fieldAValue = parseFloat($('#id_field_a').val()) || 0;
//             var fieldBValue = parseFloat($('#id_field_b').val()) || 0;
//             var result = fieldAValue * fieldBValue;
//             $('#id_computed_field').val(result.toFixed(2)); // Update the computed field
//         }

//         // Attach the event listener to the fields
//         $('#id_field_a, #id_field_b').on('change keyup', calculateField);
//     });
// })(django.jQuery);
